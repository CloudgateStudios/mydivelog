pnpm-lock.yaml
dist/
.next/
.turbo/
coverage/
fixtures/**/*.uddf
fixtures/**/*.xlsx

# Hand-authored prose. Prettier pads markdown tables, which produces large
# diffs that bury substantive edits and re-churn on every table change.
docs/**/*.md
fixtures/README.md
README.md

packages/db/src/generated/

# Next.js rewrites these on every build, in its own formatting. Letting
# Prettier reformat them means format:check fails after any build.
apps/*/tsconfig.json
apps/*/next-env.d.ts
