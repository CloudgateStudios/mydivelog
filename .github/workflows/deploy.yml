name: Deploy

# dev  — every push to main, automatically.
# prod — manual only, gated by the `prod` GitHub Environment's required
#        reviewers. Nothing reaches production without a human approving it.
on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        description: Environment to deploy
        required: true
        type: choice
        options: [dev, prod]
      services:
        description: Comma-separated services, or "all"
        required: false
        default: all
        type: string

permissions:
  contents: read

concurrency:
  # One deploy per environment at a time. Never cancel in progress — a killed
  # deploy can leave machines half-updated.
  group: deploy-${{ inputs.environment || 'dev' }}
  cancel-in-progress: false

jobs:
  resolve:
    name: Resolve targets
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.pick.outputs.environment }}
      services: ${{ steps.pick.outputs.services }}
    steps:
      - id: pick
        env:
          INPUT_ENV: ${{ inputs.environment }}
          INPUT_SERVICES: ${{ inputs.services }}
        run: |
          set -euo pipefail
          env_name="${INPUT_ENV:-dev}"
          requested="${INPUT_SERVICES:-all}"
          if [ "$requested" = "all" ] || [ -z "$requested" ]; then
            services='["api","worker","web","admin"]'
          else
            services=$(printf '%s' "$requested" | tr ',' '\n' | sed 's/^ *//;s/ *$//' \
              | grep -v '^$' | jq -R . | jq -sc .)
          fi
          echo "environment=$env_name" >> "$GITHUB_OUTPUT"
          echo "services=$services"   >> "$GITHUB_OUTPUT"
          echo "Deploying $services to $env_name"

  deploy:
    name: ${{ matrix.service }} → ${{ needs.resolve.outputs.environment }}
    needs: resolve
    runs-on: ubuntu-latest
    # Secrets resolve from this GitHub Environment, so `dev` and `prod` hold
    # different FLY_TOKEN_* values under identical names.
    environment: ${{ needs.resolve.outputs.environment }}
    strategy:
      # Sequential. The API's Fly release_command applies migrations before its
      # machines cut over, and two services racing the same database is not
      # worth the few seconds saved. Nothing here runs migrations directly: a
      # CI-only step would be skipped by a manual `fly deploy`, and would need
      # the production database URL as a GitHub secret.
      max-parallel: 1
      fail-fast: true
      matrix:
        service: ${{ fromJSON(needs.resolve.outputs.services) }}
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      # Pinned to a commit, not @master. This action runs with a Fly deploy
      # token, and a branch reference means whatever was pushed to it last.
      # ed8efb3 is the tip of both master and tag 1.6 at time of pinning.
      - uses: superfly/flyctl-actions/setup-flyctl@ed8efb33836e8b2096c7fd3ba1c8afe303ebbff1 # 1.6

      - name: Deploy ${{ matrix.service }}
        env:
          # Per-app deploy tokens: a compromised token can touch one app in one
          # environment, not the whole organization.
          FLY_API_TOKEN: ${{ secrets[format('FLY_TOKEN_{0}', matrix.service)] }}
          ENVIRONMENT: ${{ needs.resolve.outputs.environment }}
          SERVICE: ${{ matrix.service }}
        run: |
          set -euo pipefail
          if [ -z "${FLY_API_TOKEN:-}" ]; then
            echo "::error::FLY_TOKEN_${SERVICE} is not set in the '${ENVIRONMENT}' environment"
            exit 1
          fi
          # Build context is the repository root: the Dockerfiles need the whole
          # workspace to resolve pnpm workspace dependencies.
          flyctl deploy . \
            --config "infra/fly/${SERVICE}.${ENVIRONMENT}.toml" \
            --dockerfile "apps/${SERVICE}/Dockerfile" \
            --app "mydivelog-${SERVICE}-${ENVIRONMENT}" \
            --strategy rolling \
            --wait-timeout 5m

  smoke:
    name: Smoke test
    needs: [resolve, deploy]
    runs-on: ubuntu-latest
    steps:
      - name: Check public health endpoints
        env:
          ENVIRONMENT: ${{ needs.resolve.outputs.environment }}
        run: |
          set -uo pipefail
          if [ "$ENVIRONMENT" = "prod" ]; then
            base_api="https://api.mydivelog.app"
            base_web="https://mydivelog.app"
            base_admin="https://admin.mydivelog.app"
          else
            base_api="https://api-dev.mydivelog.app"
            base_web="https://dev.mydivelog.app"
            base_admin="https://admin-dev.mydivelog.app"
          fi
          fail=0
          check() {
            body=$(curl -fsS --max-time 20 --retry 5 --retry-delay 5 --retry-all-errors "$2" || true)
            # Assert the service names itself. A 200 alone proves only that
            # *something* answered — which has already fooled us once.
            if printf '%s' "$body" | grep -q "\"service\":\"$3\""; then
              echo "ok   $1 -> $body"
            else
              echo "::error::$1 health check failed at $2 (got: ${body:-no response})"
              fail=1
            fi
          }
          check api "$base_api/health"     api
          check web "$base_web/api/health" web

          # Admin sits behind Cloudflare Access, so it must NOT answer publicly.
          # Asserting the challenge is a stronger check than a 200 would be: it
          # proves both that the hostname routes and that Access is still
          # enforcing. Whether admin is actually healthy is already gated by
          # Fly's own check, which blocks the rolling deploy above.
          admin_url="$base_admin/api/health"
          admin_out=$(curl -sS -o /tmp/admin.txt -w '%{http_code} %{redirect_url}' \
            --max-time 20 --retry 3 --retry-delay 5 "$admin_url" 2>/dev/null || echo "000 ")
          admin_body=$(head -c 400 /tmp/admin.txt 2>/dev/null || true)
          if printf '%s' "$admin_body" | grep -q '"service":"admin"'; then
            echo "::error::$admin_url served its health payload publicly — Cloudflare Access is not protecting admin"
            fail=1
          elif printf '%s %s' "$admin_out" "$admin_body" | grep -qiE 'cloudflareaccess|/cdn-cgi/access|^(302|401|403) '; then
            echo "ok   admin -> Access challenge ($admin_out)"
          else
            echo "::error::$admin_url gave neither a health payload nor an Access challenge: $admin_out"
            fail=1
          fi

          # The worker is deliberately not public; Fly's own machine check
          # covers it and a failing check blocks the rolling deploy above.
          exit $fail
